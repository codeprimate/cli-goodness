create git commit hook setup
	quickly make standard hooks for new or exisitng git repos


MAKE POST COMMIT MESSAGES TO TWITTER

/.git/hooks/post-commit

#!/bin/sh
#
# An example hook script that is called after a successful
# commit is made.
#
# To enable this hook, make this file executable.

projectdesc=$(sed -e '1q' "$GIT_DIR/description")
twit "just committed files to $projectdesc"